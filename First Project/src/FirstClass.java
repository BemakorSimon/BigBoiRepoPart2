/**
 * 
 * @author Simon Gajewski
 * First Submission
 */
/**
 * 
 * @author sgajewski20
 *initalize the variable... 
 *make constants for reused numbers 
 *final double PA_SALESTAX = 0.06 -- constant wont let you change 
 *all caps is naming convention 
 *
 *String is a data type
 *you can create a string object -- it is a class
 *you can make any class a adata type
 *a variable can be assigned a string literal 
 *String Value = "Hello"
 *a cariable can also be given A NEW KEYWORD 
 *String value = new String("Hello")
 *this is initialization and rename 
 *
 *Classes have methods in them 
 *you can use length in a class 
 *stringSize = value.length()
 *
 *Scope = where that variable exists and what has access to it 
 *Scanner calss lets us  read input from the keyboard 
 *import java.util.Scanner;
 *to create a scanner object : 
 *Scanner keyboard = new Scanner (System.in);
 *method we will start with is nextLine()
 *reads until user presses enter 
 *
 *
 *
 */
public class FirstClass 

import java.util.Scanner;

{

	public static void main(String[] args);
	{
		Scanner jawn = new Scanner(System.in);
		System.out.print("please enter your name");
		String name = jawn.nextline();
	}

}

